api.array :version_statuses do
  @version_statuses.each do |status|
    api.version_status do
      api.id status.id
      api.name status.name
      api.is_closed status.is_closed
      api.description status.description
    end
  end
end
